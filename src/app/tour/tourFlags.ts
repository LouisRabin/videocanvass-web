/** Master switch: set true to show Tour buttons, overlay, and first-run banner again. */
export const TOUR_UI_ENABLED = false

/** When `TOUR_UI_ENABLED` is true: set false to hide only the Cases “Take a tour?” banner. */
export const SHOW_TOUR_FIRST_RUN_PROMPT = true
